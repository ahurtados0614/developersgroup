{#
/**
 * @file
 * Theme override to display a block.
 *
 * Available variables:
 * - plugin_id: The ID of the block implementation.
 * - label: The configured label of the block if visible.
 * - configuration: A list of the block's configuration values.
 *   - label: The configured label for the block.
 *   - label_display: The display settings for the label.
 *   - provider: The module or other provider that provided this block plugin.
 *   - Block plugin specific settings will also be stored here.
 * - in_preview: Whether the plugin is being rendered in preview mode.
 * - content: The content of this block.
 * - attributes: array of HTML attributes populated by modules, intended to
 *   be added to the main container tag of this template.
 *   - id: A valid HTML ID and guaranteed unique.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to
 *   be displayed after the main title tag that appears in the template.
 *
 * @see template_preprocess_block()
 */
#}
{%
  set classes = [
    'block',
    'block-' ~ configuration.provider|clean_class,
    'block-' ~ plugin_id|clean_class,
  ]
%}

<!-- Latest Projects-->
<section class="section section-sm section-fluid bg-default text-center">
  <div class="container-fluid isotope-wrap">
    {% if label %}
      <h2 class="wow fadeInLeft">{{ label }}</h2>
    {% endif %}
    <p class="quote-jean wow fadeInRight es" data-wow-delay=".1s">En nuestro portafolio podrá explorar los últimos productos desarrollados para nuestros clientes para diferentes fines corporativos. Nuestro equipo calificado de diseñadores de interfaces y desarrolladores de software siempre está listo para crear algo único para usted.</p>
    <p class="quote-jean wow fadeInRight en" style="display:none" data-wow-delay=".1s">In our portfolio, you can browse the latest products developed for our clients for different corporate purposes. Our qualified team of interface designers and software developers is always ready to create something unique for you.</p>
    
    <!---desktop-->
    <ul  class=" filter-desktop isotope-filters-list" id="isotope-3">
        
    </ul>
    <!---movil-->
    <div style="display:none" class="filter-movil dropdown isotope-filters isotope-filters-horizontal">
        <button class="isotope-filters-toggle button button-md button-icon button-icon-right button-default-outline button-wapasha dropdown-toggle show" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
           <span class="icon fa fa-caret-down"></span>  Filter
        </button>
        <ul class="dropdown-menu" aria-labelledby="dropdownMenuButton" style="position: absolute; inset: 0px auto auto 0px; margin: 0px; transform: translate3d(95px, 52.6667px, 0px);" data-popper-placement="bottom-start" id="itemss-filter">
            
        </ul>
    </div>

    
    <div class="gallery-wrap">
      <div id="gallery">
           
      </div>
    </div>
  </div>
</section>

<script src='https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.0/jquery.min.js'></script>
<script src="https://cdn.jsdelivr.net/npm/mixitup@3/dist/mixitup.min.js"></script>

<script>
/* 🔥 FUNCIÓN PARA LIMPIAR CLASE CSS */
const limpiarClase = (texto) => {
  return texto
    .toLowerCase()
    .normalize("NFD")                // quita acentos
    .replace(/[\u0300-\u036f]/g, "") // quita acentos
    .replace(/\s+/g, '')             // quita espacios
};
function listar_gallery() {
  let url2 = "/project-gallery";

  let galleryHTML = "";
  let filtrosDesktop = "";
  let filtrosMovil = "";
  let categorias = [];

  /* BOTÓN ALL */
    filtrosDesktop += `
      <li><a class="filter" data-filter="all">ALL</a></li>
    `;

    filtrosMovil += `
      <li><a class="dropdown-item filter" data-filter="all">ALL</a></li>
    `;

  fetch(url2)
    .then(res => res.json())
    .then(data => {

      data.forEach((element) => {

        /* 🔥 SPLIT POR PUNTO */
        let categoriasArray = element.category.split('.');

        /* 🔥 GENERAR CLASES */
        let clases = categoriasArray.map(cat => limpiarClase(cat)).join(' ');

        /* GALERÍA */
        galleryHTML += `
          <div class="gallery-item mix ${clases}">
            <div class="inside">
              <div class="details">
                <div class="text">
                  <h2>${element.title}</h2>
                  <p>${element.category.split('.').join(' | ')}</p>
                </div>
              </div>
              <div class="overlay"></div>
              <img src="${element.image}" alt="${element.title}">
            </div>
          </div>
        `;

        /* FILTROS */
        categoriasArray.forEach(cat => {

          let catClean = limpiarClase(cat);

          if (!categorias.includes(catClean) && catClean !== 'all') {

            categorias.push(catClean);

            filtrosDesktop += `
              <li>
                <a class="filter" data-filter=".${catClean}">
                  ${cat}
                </a>
              </li>
            `;

            filtrosMovil += `
              <li>
                <a class="dropdown-item filter" data-filter=".${catClean}">
                  ${cat}
                </a>
              </li>
            `;
          }

        });

      });

      document.getElementById("gallery").innerHTML = galleryHTML;
      document.getElementById("isotope-3").innerHTML = filtrosDesktop;
      document.getElementById("itemss-filter").innerHTML = filtrosMovil;

      mixitup('#gallery', {
        selectors: {
          target: '.mix'
        },
        load: {
          filter: 'all'
        }
      });

    })
    .catch(err => console.error(err));
}

document.addEventListener("DOMContentLoaded", listar_gallery);
</script>
<style>
a.filter.mixitup-control-active {
    color: #756aee !important;
}

a.filter {
    cursor: pointer;
}

/* ===== GALERÍA ===== */
.gallery-wrap {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
}

#gallery{
  display: flex;
  flex-wrap: wrap; /* 🔹 IMPORTANTE */
  justify-content: center;
}

.gallery-item {
  width: 33.333%;
  padding: 10px;
  position: relative;
}

/* 🔥 CONTENEDOR CORRECTO */
.inside {
  position: relative;
  overflow: hidden;
  width: 100%;
}

/* 🔥 IMAGEN CORREGIDA */
.inside img {
  width: 100%;
  display: block;   /* 🔹 evita espacios raros */
  height: auto;
}

/* 🔥 OVERLAY Y DETAILS */
.details,
.overlay {
  position: absolute;
  inset: 0; /* 🔹 reemplaza top/left/right/bottom */
  opacity: 0;
}

/* 🔥 OVERLAY PERFECTO */
.overlay {
  background: rgba(31, 186, 231, 0.8);
  z-index: 1;
  transition: all 0.4s ease-in-out;
}

/* 🔥 TEXTO ENCIMA */
.details {
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 2;
  transition: all 0.3s ease-in-out;
}

.details h2 {
  color: #fff;
  font-size: 1.5em;
  font-weight: 700;
  text-align: center;
  margin: 0;
}
.details h2 {
    text-transform: uppercase !important;
}
.details p {
  color: #fff;
  font-size: 1em;
  text-align: center;
  margin: 0;
  text-transform: uppercase;
}

/* 🔥 HOVER */
.gallery-item:hover .details,
.gallery-item:hover .overlay {
  opacity: 1;
}
ul#itemss-filter {
    max-width: 280px;
    width: 100%;
    padding: 20px !important;
    background: #ffffff;
    box-shadow: 0 0 10px 0 rgba(0, 0, 0, 0.17);
    transform: translate3d(-50%, 0, 0);
}
a.dropdown-item.filter{
color: #9b9b9b !important;
font-size: 18px !important;
    line-height: 1 !important;
    font-weight: 500 !important;
    letter-spacing: .1em !important;
    text-transform: uppercase !important;
    font-family: "Poppins", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif !important;
    
}
a.dropdown-item.filter.active.mixitup-control-active{
  color: #fff !important;
}
/* ===== RESPONSIVE ===== */
@media (max-width: 900px) {
  .gallery-item {
    width: 100%;
  }
  .filter-movil.dropdown.isotope-filters.isotope-filters-horizontal {
    display: block !important;
  }
  a.dropdown-item.filter.mixitup-control-active{
    color: #756aee !important;
  }
}
</style>